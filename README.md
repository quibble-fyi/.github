# quibble-fyi/.github

The organization profile shown at https://github.com/quibble-fyi.

This is the **only public repository** in the quibble-fyi organization. It holds the
profile README and the logo — nothing else, and never code. Every other repository is
private. See `docs/brand.md` in quibble-site for the voice and claims this text follows.
